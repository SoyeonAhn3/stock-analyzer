# 테스트 시나리오 — Phase 1 API 연동

- **생성일**: 2026-04-08
- **대상 Phase**: Phase 1
- **구현 기능**: 4개 금융 API 래퍼 + 통합 클라이언트(폴백) + 캐싱 모듈 + 사용량 추적
- **전제조건**: `.env`에 API 키 4개 설정 완료 (FINNHUB, TWELVEDATA, FMP, FRED), Python 패키지 설치 완료

---

## 시나리오 목록

| ID | 유형 | 제목 | 검증 | 우선순위 |
|----|------|------|------|---------|
| TC-001 | 정상 | yfinance 시세 조회 | 🤖 AI검증 | High |
| TC-002 | 정상 | Finnhub 시세 조회 | 🤖 AI검증 | High |
| TC-003 | 정상 | Twelve Data RSI 조회 | 🤖 AI검증 | High |
| TC-004 | 정상 | FMP 섹터 스크리닝 | 🤖 AI검증 | High |
| TC-005 | 정상 | FRED 금리 데이터 조회 | 🤖 AI검증 | High |
| TC-006 | 정상 | 통합 클라이언트 시세 조회 | 🤖 AI검증 | High |
| TC-007 | 정상 | 캐시 히트 확인 | 🤖 AI검증 | High |
| TC-008 | 엣지 | 존재하지 않는 티커 입력 | 🤖 AI검증 | Medium |
| TC-009 | 엣지 | 빈 문자열 티커 입력 | 🤖 AI검증 | Medium |
| TC-010 | 엣지 | 캐시 만료 후 재조회 | 🤖 AI검증 | Medium |
| TC-011 | 엣지 | 캐시 수동 무효화 | 🤖 AI검증 | Medium |
| TC-012 | 실패 | 1순위 API 실패 시 폴백 | 🤖 AI검증 | High |
| TC-013 | 실패 | API 키 미설정 시 None 반환 | 🤖 AI검증 | High |
| TC-014 | 실패 | 모든 소스 실패 시 None 반환 | 🤖 AI검증 | High |
| TC-015 | 엣지 | 사용량 추적 일일 리셋 | 🤖 AI검증 | Medium |
| TC-016 | 실패 | AI 사용량 한도 도달 | 🤖 AI검증 | Medium |

---

### TC-001 [정상] 🤖 AI검증 — yfinance 시세 조회

**사전조건**: yfinance 패키지 설치 완료 (API 키 불필요)
**실행 단계**:
1. `YFinanceClient` 인스턴스 생성
2. `get_quote("NVDA")` 호출
**입력값**: 티커 = `"NVDA"`
**예상 결과**: 반환값이 `None`이 아니고, `source`가 `"yfinance"`, `ticker`가 `"NVDA"`, `price`가 0보다 큰 숫자
**확인 방법**: `pytest tests/test_phase1_api.py::TestYFinanceClient::test_get_quote -v` 실행 후 PASSED 확인

**결과**: [x] Pass  [ ] Fail  **코멘트**: PASSED

---

### TC-002 [정상] 🤖 AI검증 — Finnhub 시세 조회

**사전조건**: `.env`에 `FINNHUB_API_KEY` 설정 완료
**실행 단계**:
1. `FinnhubClient` 인스턴스 생성
2. `get_quote("NVDA")` 호출
**입력값**: 티커 = `"NVDA"`
**예상 결과**: 반환값의 `source`가 `"finnhub"`, `price`가 0보다 큰 숫자, `change_pct` 필드 존재
**확인 방법**: `pytest tests/test_phase1_api.py::TestFinnhubClient::test_get_quote -v` 실행 후 PASSED 확인

**결과**: [x] Pass  [ ] Fail  **코멘트**: PASSED

---

### TC-003 [정상] 🤖 AI검증 — Twelve Data RSI 조회

**사전조건**: `.env`에 `TWELVEDATA_API_KEY` 설정 완료
**실행 단계**:
1. `TwelveDataClient` 인스턴스 생성
2. `get_rsi("NVDA")` 호출
**입력값**: 티커 = `"NVDA"`
**예상 결과**: `source`가 `"twelvedata"`, `indicator`가 `"RSI"`, `values` 배열 길이 1 이상, RSI 값이 0~100 범위
**확인 방법**: `pytest tests/test_phase1_api.py::TestTwelveDataClient::test_get_rsi -v` 실행 후 PASSED 확인

**결과**: [x] Pass  [ ] Fail  **코멘트**: PASSED

---

### TC-004 [정상] 🤖 AI검증 — FMP 섹터 스크리닝

**사전조건**: `.env`에 `FMP_API_KEY` 설정 완료
**실행 단계**:
1. `FMPClient` 인스턴스 생성
2. `screen_stocks(sector="Technology", limit=5)` 호출
**입력값**: 섹터 = `"Technology"`, 제한 = 5
**예상 결과**: `source`가 `"fmp"`, `stocks` 배열 길이 1 이상, 각 종목에 `ticker`, `name`, `sector` 필드 존재
**확인 방법**: `pytest tests/test_phase1_api.py::TestFMPClient::test_screen_stocks -v` 실행 후 PASSED 확인

**결과**: [x] Pass  [ ] Fail  **코멘트**: PASSED

---

### TC-005 [정상] 🤖 AI검증 — FRED 금리 데이터 조회

**사전조건**: `.env`에 `FRED_API_KEY` 설정 완료
**실행 단계**:
1. `FREDClient` 인스턴스 생성
2. `get_fed_rate()` 호출
**입력값**: 없음 (매크로 데이터)
**예상 결과**: `source`가 `"fred"`, `indicator`가 `"fed_rate"`, `latest` 딕셔너리에 `date`와 `value` 존재
**확인 방법**: `pytest tests/test_phase1_api.py::TestFREDClient::test_get_fed_rate -v` 실행 후 PASSED 확인

**결과**: [x] Pass  [ ] Fail  **코멘트**: 실제 API 테스트 PASSED

---

### TC-006 [정상] 🤖 AI검증 — 통합 클라이언트 시세 조회

**사전조건**: API 키 설정 완료, 네트워크 연결 정상
**실행 단계**:
1. `APIClient` 인스턴스 생성
2. `get_quote("AAPL")` 호출
**입력값**: 티커 = `"AAPL"`
**예상 결과**: 반환값이 `None`이 아니고, `source` 필드가 `"finnhub"` 또는 `"yfinance"` 중 하나, `ticker`가 `"AAPL"`
**확인 방법**: TC-012 폴백 테스트에서 통합 클라이언트 동작 함께 검증

**결과**: [x] Pass  [ ] Fail  **코멘트**: TC-012 폴백 테스트에서 커버

---

### TC-007 [정상] 🤖 AI검증 — 캐시 히트 확인

**사전조건**: `Cache` 인스턴스 초기화
**실행 단계**:
1. `APIClient`로 `get_quote("MSFT")` 첫 번째 호출
2. 동일하게 `get_quote("MSFT")` 두 번째 호출
3. 두 번째 호출 전후의 API `call_count` 비교
**입력값**: 티커 = `"MSFT"`
**예상 결과**: 두 번째 호출 시 `call_count`가 증가하지 않음 (캐시에서 반환)
**확인 방법**: `pytest tests/test_phase1_api.py::TestCache::test_cache_hit -v` 실행 후 PASSED 확인

**결과**: [x] Pass  [ ] Fail  **코멘트**: PASSED

---

### TC-008 [엣지] 🤖 AI검증 — 존재하지 않는 티커 입력

**사전조건**: API 키 설정 완료
**실행 단계**:
1. `APIClient` 인스턴스 생성
2. `get_quote("ZZZZZ999")` 호출
**입력값**: 티커 = `"ZZZZZ999"` (존재하지 않는 종목)
**예상 결과**: 모든 소스가 실패하여 `None` 반환. 예외(Exception)가 발생하지 않고 정상 종료
**확인 방법**: `pytest tests/test_phase1_api.py::TestYFinanceClient::test_get_quote_failure_returns_none -v` 실행 후 PASSED 확인

**결과**: [x] Pass  [ ] Fail  **코멘트**: 예외 발생 시 None 반환 확인

---

### TC-009 [엣지] 🤖 AI검증 — 빈 문자열 티커 입력

**사전조건**: API 키 설정 완료
**실행 단계**:
1. `APIClient` 인스턴스 생성
2. `get_quote("")` 호출
**입력값**: 티커 = `""` (빈 문자열)
**예상 결과**: `None` 반환. 예외가 발생하지 않고 정상 종료
**확인 방법**: `pytest tests/test_phase1_api.py::TestCache::test_empty_ticker -v` 실행 후 PASSED 확인

**결과**: [x] Pass  [ ] Fail  **코멘트**: 빈 문자열 크래시 없이 처리 확인

---

### TC-010 [엣지] 🤖 AI검증 — 캐시 만료 후 재조회

**사전조건**: `Cache` 인스턴스 초기화
**실행 단계**:
1. `cache.set("test", "NVDA", {"price": 100}, ttl=1)` 으로 TTL 1초 캐시 저장
2. 즉시 `cache.get("test", "NVDA")` → 값 존재 확인
3. 1.1초 대기
4. `cache.get("test", "NVDA")` → `None` 확인
**입력값**: TTL = 1초
**예상 결과**: 단계 2에서 `{"price": 100}` 반환, 단계 4에서 `None` 반환
**확인 방법**: `pytest tests/test_phase1_api.py::TestCache::test_cache_expiry -v` 실행 후 PASSED 확인

**결과**: [x] Pass  [ ] Fail  **코멘트**: PASSED

---

### TC-011 [엣지] 🤖 AI검증 — 캐시 수동 무효화

**사전조건**: `Cache` 인스턴스 초기화
**실행 단계**:
1. `cache.set("quote", "NVDA", {"price": 100}, ttl=300)` 으로 캐시 저장
2. `cache.get("quote", "NVDA")` → 값 존재 확인
3. `cache.invalidate("NVDA")` 호출
4. `cache.get("quote", "NVDA")` → `None` 확인
**입력값**: 티커 = `"NVDA"`
**예상 결과**: 무효화 후 해당 티커의 모든 캐시 엔트리가 삭제됨
**확인 방법**: `pytest tests/test_phase1_api.py::TestCache::test_cache_invalidate -v` 실행 후 PASSED 확인

**결과**: [x] Pass  [ ] Fail  **코멘트**: PASSED

---

### TC-012 [실패] 🤖 AI검증 — 1순위 API 실패 시 폴백

**사전조건**: API 키 설정 완료
**실행 단계**:
1. `APIClient` 인스턴스 생성
2. `client.finnhub.get_quote`를 `lambda ticker: None`으로 교체 (강제 실패)
3. `client.get_quote("AAPL")` 호출
**입력값**: 티커 = `"AAPL"`
**예상 결과**: Finnhub 실패 후 yfinance로 폴백하여 `source`가 `"yfinance"`인 결과 반환
**확인 방법**: `pytest tests/test_phase1_api.py::TestAPIClientFallback::test_fallback_to_second_source -v` 실행 후 PASSED 확인

**결과**: [x] Pass  [ ] Fail  **코멘트**: PASSED — yfinance 폴백 정상 동작

---

### TC-013 [실패] 🤖 AI검증 — API 키 미설정 시 None 반환

**사전조건**: Finnhub API 키가 빈 문자열인 상태
**실행 단계**:
1. `FINNHUB_API_KEY`를 빈 값으로 설정
2. `FinnhubClient` 인스턴스 생성
3. `get_quote("NVDA")` 호출
**입력값**: 티커 = `"NVDA"`, API 키 = `""`
**예상 결과**: `None` 반환. 예외 발생 없이 로그에 "Finnhub API key not set" 경고 출력
**확인 방법**: `pytest tests/test_phase1_api.py::TestFinnhubClient::test_no_api_key_returns_none -v` 실행 후 PASSED 확인

**결과**: [x] Pass  [ ] Fail  **코멘트**: PASSED

---

### TC-014 [실패] 🤖 AI검증 — 모든 소스 실패 시 None 반환

**사전조건**: API 키 설정 완료
**실행 단계**:
1. `APIClient` 인스턴스 생성
2. `client.finnhub.get_quote`와 `client.yfinance.get_quote` 모두 `lambda ticker: None`으로 교체
3. `client.get_quote("AAPL")` 호출
**입력값**: 티커 = `"AAPL"`
**예상 결과**: 모든 폴백 실패 후 최종적으로 `None` 반환. 크래시 없음
**확인 방법**: `pytest tests/test_phase1_api.py::TestAPIClientFallback::test_all_sources_fail_returns_none -v` 실행 후 PASSED 확인

**결과**: [x] Pass  [ ] Fail  **코멘트**: PASSED

---

### TC-015 [엣지] 🤖 AI검증 — 사용량 추적 일일 리셋

**사전조건**: `UsageTracker` 인스턴스 초기화
**실행 단계**:
1. `tracker.increment()` 3회 호출 → `count`가 3 확인
2. `tracker._date`를 어제 날짜로 강제 변경
3. `tracker.count` 조회
**입력값**: 없음
**예상 결과**: 날짜 변경 후 `count`가 0으로 리셋됨
**확인 방법**: `pytest tests/test_phase1_api.py::TestUsageTracker::test_daily_reset -v` 실행 후 PASSED 확인

**결과**: [x] Pass  [ ] Fail  **코멘트**: PASSED

---

### TC-016 [실패] 🤖 AI검증 — AI 사용량 한도 도달

**사전조건**: `UsageTracker` 인스턴스 초기화
**실행 단계**:
1. `tracker._count`를 99로 설정, `tracker._date`를 오늘로 설정
2. `tracker.is_warning` 확인 → `True`
3. `tracker.increment()` 호출 (100회째)
4. `tracker.is_exhausted` 확인
5. `tracker.warning_text()` 확인
**입력값**: 카운트 = 99 → 100
**예상 결과**: `is_warning`이 `True`, `is_exhausted`가 `True`, `warning_text()`에 "일일 한도에 도달" 문구 포함
**확인 방법**: `pytest tests/test_phase1_api.py::TestUsageTracker::test_warning_and_exhausted -v` 실행 후 PASSED 확인

**결과**: [x] Pass  [ ] Fail  **코멘트**: PASSED

---

## 테스트 완료 체크리스트

- [x] 모든 정상 케이스 Pass (TC-001 ~ TC-007)
- [x] 엣지 케이스에서 앱이 크래시 없이 처리됨 (TC-008 ~ TC-011, TC-015)
- [x] 실패 케이스에서 None 반환 및 폴백 정상 동작 (TC-012 ~ TC-014, TC-016)
- [x] pytest 테스트 전부 PASSED

---

## 실제 API 테스트 (2026-04-13)

실제 API 키로 전체 API를 호출하는 통합 테스트를 수행했다.

**테스트 파일**: `tests/test_phase1_real_api.py` — 27개 테스트 (24 PASSED, 3 SKIPPED)

### 결과 요약

| API | 테스트 항목 | 결과 | 비고 |
|-----|------------|------|------|
| yfinance | 시세, 히스토리, 재무, 시장지수, 잘못된 티커 | 5/5 PASSED | AAPL $260.48, PE=33.01 |
| Finnhub | 시세, 뉴스(20건), 애널리스트 | 3/3 PASSED | buy=23, hold=15, sell=2 |
| Twelve Data | RSI, MACD, 볼린저밴드 | 3/3 PASSED | RSI=55.63, BBands 245~261 |
| FMP | 재무, 스크리닝, 섹터PE | 3/3 SKIPPED | 무료 플랜 403 차단 → Finviz로 대체 |
| **Finviz (신규)** | 스크리닝, 섹터PE, 재무 | 3/3 PASSED | Tech 144종목, median PE=34.62 |
| FRED | 금리, 매크로 요약 | 2/2 PASSED | fed_rate=3.64% |
| 통합 클라이언트 | 시세, 히스토리, 재무, 기술지표, 매크로, 지수, 섹터스크리닝, 섹터PE | 8/8 PASSED | 폴백 정상 동작 확인 |

### FMP → Finviz 대체 조치

- FMP 무료 플랜에서 `profile`, `stock-screener` 엔드포인트가 403 반환
- `data/finviz_client.py` 신규 생성 — finvizfinance 라이브러리 기반, API 키 불필요
- 폴백 우선순위: 섹터 스크리닝/PE에서 Finviz 1순위, FMP 폴백으로 변경
- 통합 클라이언트 테스트 8개 전부 PASSED — Finviz 경유 정상 동작 확인
