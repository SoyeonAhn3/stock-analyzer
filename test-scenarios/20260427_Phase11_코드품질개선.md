# 테스트 시나리오 — Phase 11 코드 품질 개선

- **생성일**: 2026-04-27
- **대상 Phase**: Phase 11
- **구현 기능**: Code Review 기반 보안·성능·코드품질 Top 10 이슈 수정
- **전제조건**: 백엔드 서버 실행 (`python -m backend.main`), `.env`에 API 키 설정 완료

---

## 시나리오 목록

| ID | 유형 | 제목 | 검증 | 우선순위 | 결과 |
|----|------|------|------|---------|------|
| TC-001 | 정상 | API 키 로그 마스킹 동작 확인 | 🤖 AI검증 | High | ✅ Pass |
| TC-002 | 정상 | AI 응답 화이트리스트 필터링 | 🤖 AI검증 | High | ✅ Pass |
| TC-003 | 정상 | Claude 클라이언트 싱글턴 재사용 | 🤖 AI검증 | High | ✅ Pass |
| TC-004 | 정상 | 시장 지수 6개 병렬 조회 | 👤 수동 | High | ⏳ 미실행 |
| TC-005 | 엣지 | 시장 지수 부분 실패 시 성공분만 반환 | 🤖 AI검증 | Medium | ✅ Pass |
| TC-006 | 엣지 | 매크로 데이터 전체 실패 시 None 반환 | 🤖 AI검증 | Medium | ✅ Pass |
| TC-007 | 실패 | AI 응답 JSON 파싱 실패 시 raw_output 보존 | 🤖 AI검증 | High | ✅ Pass |
| TC-008 | 실패 | API 키 미설정 시 안전한 에러 반환 | 🤖 AI검증 | Medium | ✅ Pass |

---

### TC-001 [정상] 🤖 AI검증 — API 키 로그 마스킹 동작 확인

**사전조건**: `data/sanitize.py`에 `mask_sensitive()` 함수 존재, 각 클라이언트에서 import 완료
**실행 단계**:
1. `data/sanitize.py`에 `mask_sensitive` 함수가 정의되어 있는지 확인
2. `data/finnhub_client.py`, `data/fmp_client.py`, `data/fred_client.py`에서 에러 로깅 시 `mask_sensitive()`를 호출하는지 확인
3. 각 클라이언트 `__repr__`에 API 키가 노출되지 않는지 확인
**입력값**: 해당 소스 파일
**예상 결과**: 모든 `logger.warning` 호출에서 에러 메시지가 `mask_sensitive()`로 래핑되어 있고, `__repr__`에 API 키 문자열이 포함되지 않음
**확인 방법**: Grep으로 `mask_sensitive` import 및 사용처 확인, `__repr__` 메서드 내용 검사

**결과**: [x] Pass  [ ] Fail  **코멘트**: 4개 클라이언트 모두 `mask_sensitive` import·사용 확인. `__repr__`에 API 키 미포함 확인.

---

### TC-002 [정상] 🤖 AI검증 — AI 응답 화이트리스트 필터링

**사전조건**: 5개 Agent 파일에 `_ALLOWED_KEYS` 변수 정의
**실행 단계**:
1. `agents/news_agent.py`, `agents/data_agent.py`, `agents/macro_agent.py`, `agents/analyst_agent.py`, `agents/cross_validation.py` 각각에서 `_ALLOWED_KEYS` 세트가 정의되어 있는지 확인
2. `result['data']`를 사용하는 부분에서 `{k: v for k, v in result['data'].items() if k in _ALLOWED_KEYS}` 패턴이 적용되었는지 확인
**입력값**: 해당 소스 파일 5개
**예상 결과**: 5개 Agent 모두 `_ALLOWED_KEYS` 세트가 존재하며, `**result['data']` 무검증 언패킹이 없고 화이트리스트 필터링 후 언패킹됨
**확인 방법**: Grep으로 `_ALLOWED_KEYS` 정의 5건 확인, `**result['data']` 직접 언패킹 0건 확인

**결과**: [x] Pass  [ ] Fail  **코멘트**: 5개 Agent 모두 `_ALLOWED_KEYS` 정의 + 필터링 패턴 적용 확인. `**result['data']` 무검증 언패킹 0건.

---

### TC-003 [정상] 🤖 AI검증 — Claude 클라이언트 싱글턴 재사용

**사전조건**: `agents/claude_client.py` 파일 존재
**실행 단계**:
1. 모듈 레벨에 `_client: Anthropic | None = None` 선언이 있는지 확인
2. `_get_client()` 함수가 `_client`가 None일 때만 인스턴스를 생성하는지 확인
3. `call_claude()` 내부에서 `Anthropic()`을 직접 호출하지 않고 `_get_client()`를 사용하는지 확인
**입력값**: `agents/claude_client.py`
**예상 결과**: `_client` 모듈 변수가 L26에 선언, `_get_client()`가 lazy singleton 패턴으로 구현, `call_claude()` 내부에서 `client = _get_client()` 호출
**확인 방법**: Read로 해당 파일의 싱글턴 구조 확인

**결과**: [x] Pass  [ ] Fail  **코멘트**: L26 `_client = None`, L29 `_get_client()` lazy singleton, L64 `client = _get_client()` 확인. 직접 `Anthropic()` 호출 없음.

---

### TC-004 [정상] 👤 수동 — 시장 지수 6개 병렬 조회

**사전조건**: 백엔드 서버 실행 중, 인터넷 연결 정상
**실행 단계**:
1. 브라우저에서 메인 페이지(Overview) 접속
2. 시장 지수 섹션(S&P500, NASDAQ, DOW, BTC, ETH, VIX) 로딩 확인
3. 개발자 도구(F12) → Network 탭에서 API 호출 시간 확인
**입력값**: 없음
**예상 결과**: 6개 지수가 모두 표시되며, 전체 로딩 시간이 약 1초 이내 (병렬 전 ~3초+ 대비 개선). Network 탭에서 6개 요청이 거의 동시에 시작됨
**확인 방법**: 브라우저 Network 탭에서 요청 시작 시각이 거의 동일한지 육안 확인, 전체 응답 시간 측정

**결과**: [ ] Pass  [ ] Fail  **코멘트**: ___

---

### TC-005 [엣지] 🤖 AI검증 — 시장 지수 부분 실패 시 성공분만 반환

**사전조건**: `data/yfinance_client.py` 파일 존재
**실행 단계**:
1. `get_market_indices()` 메서드에서 개별 지수 조회 실패 시 해당 항목만 건너뛰는 로직 확인
2. `_fetch_one()` 내부에서 예외 발생 시 `(name, None)` 반환하는지 확인
3. 결과 조합 시 `data is not None`인 항목만 `result` dict에 포함하는지 확인
**입력값**: `data/yfinance_client.py`의 `get_market_indices()` 메서드
**예상 결과**: `_fetch_one()`이 개별 예외를 catch하고 `(name, None)` 반환, 메인 로직에서 `if data is not None` 조건으로 필터링, 전체 실패 시에만 `None` 반환
**확인 방법**: Read로 해당 메서드 코드 구조 확인

**결과**: [x] Pass  [ ] Fail  **코멘트**: `_fetch_one()` 내부 try/except → `(name, None)` 반환. L137 `if data is not None` 필터링. L139 전체 실패 시 `None` 반환 확인.

---

### TC-006 [엣지] 🤖 AI검증 — 매크로 데이터 전체 실패 시 None 반환

**사전조건**: `data/fred_client.py` 파일 존재
**실행 단계**:
1. `get_macro_summary()` 메서드에서 4개 API를 `ThreadPoolExecutor`로 병렬 호출하는지 확인
2. 모든 결과가 None일 때 (`not any([fed, cpi, unemp, spread])`) `None`을 반환하는지 확인
3. 일부만 성공 시 성공한 항목만 포함하여 dict를 반환하는지 확인
**입력값**: `data/fred_client.py`의 `get_macro_summary()` 메서드
**예상 결과**: `ThreadPoolExecutor(max_workers=4)` 사용, 전체 실패 시 `None` 반환, 부분 성공 시 해당 키만 값이 있는 dict 반환
**확인 방법**: Read로 병렬 호출 구조 및 None 체크 로직 확인

**결과**: [x] Pass  [ ] Fail  **코멘트**: `ThreadPoolExecutor(max_workers=4)` 4개 병렬 호출. L111 `not any(...)` 시 `None` 반환. 부분 성공 시 `fed["latest"] if fed else None` 패턴 확인.

---

### TC-007 [실패] 🤖 AI검증 — AI 응답 JSON 파싱 실패 시 raw_output 보존

**사전조건**: `agents/claude_client.py` 파일 존재
**실행 단계**:
1. `_parse_json_response()` 함수에서 3단계 파싱 시도(전체 JSON → ```json 블록 → 바깥 중괄호) 구조 확인
2. 3단계 모두 실패 시 `{"parsed": False, "raw_output": raw_text, "error": "JSON 파싱 실패"}` 형태로 반환하는지 확인
3. `news_agent.py`에서 `raw_output`이 있을 때 `status: "partial"`로 부분 성공 처리하는지 확인
**입력값**: `agents/claude_client.py`, `agents/news_agent.py`
**예상 결과**: 파싱 실패 시 원본 텍스트가 `raw_output` 키에 보존되고, Agent 레벨에서 `partial` 상태로 500자 요약과 함께 반환됨
**확인 방법**: Read로 `_parse_json_response` 반환값 구조 및 Agent의 `raw_output` 처리 로직 확인

**결과**: [x] Pass  [ ] Fail  **코멘트**: 3단계 파싱 시도(전체→json블록→중괄호) 확인. 실패 시 L126-130 `raw_output: raw_text` 보존. `news_agent.py` L75 `status: "partial"` + 500자 요약 처리 확인.

---

### TC-008 [실패] 🤖 AI검증 — API 키 미설정 시 안전한 에러 반환

**사전조건**: `agents/claude_client.py`, `data/fred_client.py` 파일 존재
**실행 단계**:
1. `call_claude()`에서 `ANTHROPIC_API_KEY`가 빈 값일 때 API 호출 없이 즉시 에러 dict를 반환하는지 확인
2. `FREDClient._get_series()`에서 `FRED_API_KEY`가 없을 때 `None`을 반환하는지 확인
3. `FinnhubClient._get()`에서 `FINNHUB_API_KEY`가 없을 때 `None`을 반환하는지 확인
**입력값**: 해당 소스 파일 3개
**예상 결과**: 각 클라이언트가 API 키 미설정 시 예외를 발생시키지 않고, `None` 또는 `{"parsed": False, ...}` 형태의 안전한 응답을 반환
**확인 방법**: Read로 각 함수 상단의 API 키 체크 guard 절 확인

**결과**: [x] Pass  [ ] Fail  **코멘트**: `call_claude()` L56 ANTHROPIC_API_KEY 빈값 guard, `FREDClient._get_series()` L40 FRED_API_KEY guard, `FinnhubClient._get()` L30 FINNHUB_API_KEY guard — 모두 예외 없이 안전 반환.

---

## 테스트 완료 체크리스트

- [x] 모든 정상 케이스 Pass (TC-004 수동 제외)
- [x] 엣지 케이스에서 앱이 크래시 없이 처리됨
- [x] 실패 케이스에서 사용자에게 적절한 안내가 표시됨
