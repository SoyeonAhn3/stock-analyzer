# 테스트 시나리오 — Phase 14 무료체험 (Google 로그인 게이트)

- **생성일**: 2026-06-23
- **대상 Phase**: Phase 14
- **구현 기능**: Google 로그인 게이트 무료체험 — 계정당 무료 AI 분석 3회, "AI 분석" 클릭 시에만 로그인 요구, 지갑/크레딧 차감·환불·429 한도모달
- **전제조건**: 백엔드 `http://127.0.0.1:8001` + 프론트 `http://localhost:5173` 기동, `.env` `GOOGLE_CLIENT_ID` / `frontend/.env` `VITE_GOOGLE_CLIENT_ID` 설정 완료

---

## ⚠️ 테스트 환경 구분

| 표시 | 의미 |
|------|------|
| 🟢 사내망 OK | 금융데이터 없이 검증 가능 — 로컬에서 지금 바로 |
| 🔴 외부망 필요 | 실제 AI 분석 실행이 필요 — 사내망은 Yahoo 등 금융 API 차단으로 분석 불가. **집/외부망 로컬** 또는 **배포본(Netlify+Render)** 에서 수행 |

> 사내망에서 시세/차트가 `--` 로 뜨는 것은 Phase 14 버그가 아니라 데이터 API 차단 때문임.

---

## 시나리오 목록

| ID | 유형 | 제목 | 검증 | 환경 | 우선순위 |
|----|------|------|------|------|---------|
| TC-001 | 정상 | 비로그인 사이드바 TrialBanner 표시 | 👤 수동 | 🟢 사내망 OK | High |
| TC-002 | 정상 | 비로그인 "AI 분석" 클릭 → Google 로그인 게이트 | 👤 수동 | 🟢 사내망 OK | High |
| TC-003 | 정상 | 로그인 성공 → 배너 3/3 + 백엔드 토큰검증 | 👤 수동 | 🟢 사내망 OK | High |
| TC-004 | 정상 | 로그인 후 분석 실행 → 크레딧 1 차감(2/3) | 👤 수동 | 🔴 외부망 필요 | High |
| TC-005 | 엣지 | 3회 소진 → TrialLimitModal 표시 | 👤 수동 | 🔴 외부망 필요 | High |
| TC-006 | 엣지 | localStorage 초기화 후 재로그인 → 크레딧 유지 | 👤 수동 | 🔴 외부망 필요 | Medium |
| TC-007 | 엣지 | 캐시 히트 재분석 → 무차감 | 👤 수동 | 🔴 외부망 필요 | Medium |
| TC-008 | 실패 | 무효/만료 토큰으로 분석 요청 → 401 | 🤖 AI검증 | 🟢 사내망 OK | High |
| TC-009 | 실패 | 분석 파이프라인 하드 실패 → 크레딧 환불 | 🤖 AI검증 | 🟢 사내망 OK | Medium |

---

### TC-001 [정상] 👤 수동 — 비로그인 사이드바 TrialBanner 표시

**사전조건**: 로그아웃 상태(localStorage `quantai_token` 없음)로 앱 첫 진입
**실행 단계**:
1. `http://localhost:5173` 접속
2. 왼쪽 사이드바 하단 영역 확인
**입력값**: 없음
**예상 결과**: 기존 하드코딩 "AI USAGE 0/100"이 사라지고, **"로그인하고 무료 3회 받기" 문구 + Google 로그인 버튼**이 표시된다
**확인 방법**: 사이드바에 Google 로그인 버튼 노출 + "무료 3회" 안내 문구 확인

**결과**: [x] Pass  [ ] Fail  **코멘트**: 2026-06-23 사용자 확인 완료

---

### TC-002 [정상] 👤 수동 — 비로그인 "AI 분석" 클릭 → Google 로그인 게이트

**사전조건**: 로그아웃 상태
**실행 단계**:
1. 티커(예: `NVDA`) 검색 → QuickLook 진입
2. 하단 "AI 분석" 버튼 클릭
**입력값**: 티커 `NVDA`
**예상 결과**: API(`POST /analysis`)를 호출하지 않고, **Google 로그인 플로우가 먼저 열린다**(로그인 우선 게이트). 분석 스켈레톤/결과는 뜨지 않는다
**확인 방법**: 네트워크 탭에 `/analysis` 요청이 없고 Google 로그인 팝업/화면만 뜸

**결과**: [x] Pass  [ ] Fail  **코멘트**: 2026-06-23 사내망 로컬 확인 완료

---

### TC-003 [정상] 👤 수동 — 로그인 성공 → 배너 3/3 + 백엔드 토큰검증

**사전조건**: 로그아웃 상태, 유효한 Google 계정 보유
**실행 단계**:
1. 사이드바 Google 로그인 버튼 클릭 → 실제 계정으로 로그인
2. 로그인 후 사이드바 TrialBanner 상태 확인
**입력값**: 본인 Google 계정
**예상 결과**: 배너가 **유저 이름/이메일 + "3/3 remaining"(잔여 3) + 진행바 + 로그아웃 버튼**으로 전환된다. (배너의 "3/3"은 프론트가 `GET /trial/status`를 Bearer 토큰으로 호출해 `{balance:3, held:0, available:3}`를 받은 결과 → 실제 토큰검증·지갑생성이 E2E로 성공했다는 증거)
**확인 방법**: 배너에 "3/3" 표시 + 네트워크 탭 `/api/trial/status` 응답 200 / `available:3`

**결과**: [x] Pass  [ ] Fail  **코멘트**: 2026-06-23 사내망 로컬 확인 완료

---

### TC-004 [정상] 👤 수동 — 로그인 후 분석 실행 → 크레딧 1 차감(2/3)

**사전조건**: 로그인 상태(잔여 3), 외부망(금융데이터 접근 가능)
**실행 단계**:
1. 티커(예: `NVDA`) → QuickLook → "AI 분석" 클릭
2. 분석 완료(1~2분) 대기
3. 사이드바 배너 잔여 횟수 확인
**입력값**: 티커 `NVDA` (캐시에 없는 신규 분석)
**예상 결과**: 분석 결과(BUY/HOLD/SELL 등)가 표시되고, 배너 잔여가 **"2/3 remaining"**으로 줄어든다. 응답 body에 `wallet:{balance:2, held:0, available:2}` 포함
**확인 방법**: 배너 "2/3" + `/analysis` 응답의 `wallet.available` 값 확인

**결과**: [ ] Pass  [ ] Fail  **코멘트**: ___

---

### TC-005 [엣지] 👤 수동 — 3회 소진 → TrialLimitModal 표시

**사전조건**: 로그인 상태, 외부망, 서로 다른 신규 티커 3개 준비
**실행 단계**:
1. 신규 티커 3개를 각각 분석해 잔여를 0/3까지 소진
2. 4번째 신규 티커로 "AI 분석" 클릭
**입력값**: 신규 티커 4개 (모두 캐시 미스)
**예상 결과**: 4번째 요청이 **HTTP 429**로 막히고, **TrialLimitModal**("무료 3회를 모두 사용했습니다. 프리미엄 플랜 준비 중입니다." + [확인])이 뜬다. 배너는 "0/3"
**확인 방법**: 모달 문구/버튼 노출 + 네트워크 탭 `/analysis` 응답 429 `trial_limit_reached`

**결과**: [ ] Pass  [ ] Fail  **코멘트**: ___

---

### TC-006 [엣지] 👤 수동 — localStorage 초기화 후 재로그인 → 크레딧 유지

**사전조건**: 이미 1회 이상 차감된 로그인 상태(예: 2/3), 외부망
**실행 단계**:
1. F12 → Application → Local Storage → `quantai_token` 삭제 → 새로고침(로그아웃됨)
2. 같은 Google 계정으로 다시 로그인
3. 배너 잔여 확인
**입력값**: 동일 Google 계정
**예상 결과**: 크레딧이 3으로 초기화되지 않고 **이전 잔여 그대로(예: 2/3)** 표시된다 (같은 `sub` → 같은 지갑)
**확인 방법**: 배너 잔여가 초기화 전과 동일

**결과**: [ ] Pass  [ ] Fail  **코멘트**: ___

---

### TC-007 [엣지] 👤 수동 — 캐시 히트 재분석 → 무차감

**사전조건**: 로그인 상태, 외부망, 이미 분석한 티커(24h 캐시 존재)
**실행 단계**:
1. 직전에 분석한 티커를 다시 "AI 분석"(force 아님)
2. 배너 잔여 확인
**입력값**: 캐시에 있는 티커
**예상 결과**: 결과가 즉시(캐시) 표시되고 **잔여 횟수는 줄지 않는다**(캐시 히트 무차감)
**확인 방법**: 배너 잔여 변화 없음 + 응답이 빠르게 반환

**결과**: [ ] Pass  [ ] Fail  **코멘트**: ___

---

### TC-008 [실패] 🤖 AI검증 — 무효/만료 토큰으로 분석 요청 → 401

**사전조건**: 백엔드 기동
**실행 단계**:
1. 토큰 없이: `curl -X POST http://127.0.0.1:8001/api/analysis/NVDA`
2. 무효 토큰: `curl -H "Authorization: Bearer invalid.token" http://127.0.0.1:8001/api/trial/status`
**입력값**: 토큰 없음 / 잘못된 토큰 문자열
**예상 결과**: 두 경우 모두 **HTTP 401** 반환(분석 파이프라인 실행 안 됨, 크레딧 미차감)
**확인 방법**: HTTP 상태코드 401 — `backend/auth.py` `get_current_user`가 누락/무효 토큰에 401 발생시키는 로직 확인

**결과**: [x] Pass  [ ] Fail  **코멘트**: 2026-06-23 로컬 curl 확인 — 토큰없음/무효 모두 401

---

### TC-009 [실패] 🤖 AI검증 — 분석 파이프라인 하드 실패 → 크레딧 환불

**사전조건**: 코드 리뷰 또는 외부망 재현
**실행 단계**:
1. (코드) `backend/routers/analysis.py`의 예약→실행→확정/환불 분기 확인
2. (재현) 전 에이전트 실패/예외/타임아웃을 유발해 분석 실패
**입력값**: 분석 실패 유발 상황
**예상 결과**: 하드 실패 시 `release_credit`로 **보류분이 환불**되어 잔여가 줄지 않음. 부분 성공(1~2개 실패해도 결과 생성)은 `commit_credit`로 차감 유지
**확인 방법**: `analysis.py`에서 예외/전체실패 경로가 `release_credit` 호출하는지 확인 (단위테스트 `test_phase14_trial.py`의 환불 케이스 포함, 12개 통과)

**결과**: [x] Pass  [ ] Fail  **코멘트**: 2026-06-23 단위테스트 12개 통과(환불/멱등성/계정격리 포함)

---

## 테스트 완료 체크리스트

- [ ] 모든 정상 케이스 Pass (TC-001~004)
- [ ] 엣지 케이스에서 앱이 크래시 없이 처리됨 (TC-005~007)
- [x] 실패 케이스에서 적절히 차단/환불됨 (TC-008~009)
- [ ] localStorage 초기화 후에도 크레딧이 유지됨 (TC-006)
- [ ] 🔴 외부망 필요 항목(TC-004~007)은 집/외부망 또는 배포본에서 별도 수행
