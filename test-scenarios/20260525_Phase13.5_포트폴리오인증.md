# 테스트 시나리오 — 포트폴리오 인증 시스템

- **생성일**: 2026-05-25
- **대상 Phase**: Phase 13.5
- **구현 기능**: 코드+PIN 기반 인증 게이트 + 서버 포트폴리오 동기화
- **전제조건**: `npm run dev` (frontend) + `uvicorn backend.main:app --reload` (backend) 실행 상태

---

## 시나리오 목록

| ID | 유형 | 제목 | 검증 | 우선순위 |
|----|------|------|------|---------|
| TC-001 | 정상 | 신규 코드 발급 (Register → Issued) | 👤 수동 | High |
| TC-002 | 정상 | 코드+PIN 로그인 | 👤 수동 | High |
| TC-003 | 정상 | 종목 추가 → 서버 동기화 확인 | 👤 수동 | High |
| TC-004 | 정상 | 로그아웃 → 재로그인 → 데이터 복원 | 👤 수동 | High |
| TC-005 | 정상 | Settings MY ACCOUNT 표시 | 👤 수동 | Medium |
| TC-006 | 정상 | Remember 체크 → 탭 닫기 → 코드 기억 | 👤 수동 | Medium |
| TC-007 | 정상 | 빈 포트폴리오 초기 화면 | 👤 수동 | Medium |
| TC-008 | 정상 | 발급 코드 복사 버튼 | 👤 수동 | Low |
| TC-009 | 엣지 | 코드 입력 자동 포맷팅 (XXXX-XXXX-XXXX) | 🤖 AI검증 | High |
| TC-010 | 엣지 | PIN에 문자/특수문자 입력 필터링 | 🤖 AI검증 | High |
| TC-011 | 엣지 | 불완전한 코드/PIN으로 버튼 비활성 | 🤖 AI검증 | High |
| TC-012 | 엣지 | Register에서 PIN 확인 불일치 | 🤖 AI검증 | Medium |
| TC-013 | 엣지 | 서버 데이터가 빈 배열일 때 (빈 포트폴리오) | 🤖 AI검증 | Medium |
| TC-014 | 엣지 | 서버 데이터가 Array가 아닐 때 | 🤖 AI검증 | High |
| TC-015 | 실패 | 존재하지 않는 코드로 로그인 (404) | 👤 수동 | High |
| TC-016 | 실패 | 틀린 PIN 3회 → 계정 잠금 (429) | 👤 수동 | High |
| TC-017 | 실패 | 서버 다운 시 로그인 시도 | 👤 수동 | Medium |
| TC-018 | 실패 | pushSync 실패 시 에러 표시 | 🤖 AI검증 | High |
| TC-019 | 실패 | pullSync 실패 시 에러 표시 | 🤖 AI검증 | High |
| TC-020 | 실패 | 손상된 JSON 서버 응답 처리 | 🤖 AI검증 | Medium |

---

### TC-001 [정상] 👤 수동 — 신규 코드 발급 (Register → Issued)

**사전조건**: 세션 없음 (sessionStorage 비어있음), Portfolio 페이지 접속
**실행 단계**:
1. Portfolio 페이지 접속 → 로그인 게이트 표시 확인
2. "Get a Code" 링크 클릭
3. SET PIN 필드에 `1234` 입력
4. CONFIRM PIN 필드에 `1234` 입력
5. "Create" 버튼 클릭
**입력값**: PIN = `1234`, CONFIRM = `1234`
**예상 결과**:
- "Portfolio Created!" 녹색 텍스트 표시
- YOUR UNIQUE CODE 아래 `XXXX-XXXX-XXXX` 형식 코드 표시 (12자리 영숫자 + 하이픈)
- "Copy" 버튼과 "Start Portfolio" 버튼 표시
- "Save this code!" 경고 메시지 주황색 표시
**확인 방법**: 화면에 코드가 표시되고, DevTools > Application > Session Storage에 `portfolio_session` 키가 생성됨

**결과**: [ ] Pass  [ ] Fail  **코멘트**: ___

---

### TC-002 [정상] 👤 수동 — 코드+PIN 로그인

**사전조건**: TC-001에서 발급받은 코드 기록, 로그아웃 또는 새 탭
**실행 단계**:
1. Portfolio 페이지 접속 → 로그인 게이트 표시
2. CODE 필드에 발급받은 코드 입력
3. PIN 필드에 `1234` 입력
4. "Login" 버튼 클릭
**입력값**: CODE = TC-001에서 발급받은 코드, PIN = `1234`
**예상 결과**:
- 로그인 게이트가 사라지고 포트폴리오 본문 화면 표시
- 헤더에 "Portfolio" 제목 아래 코드가 모노스페이스 폰트로 표시
- Refresh / + Add Stock / Logout 버튼 3개 표시
**확인 방법**: 포트폴리오 메인 화면 진입, 헤더의 코드가 발급받은 코드와 일치

**결과**: [ ] Pass  [ ] Fail  **코멘트**: ___

---

### TC-003 [정상] 👤 수동 — 종목 추가 → 서버 동기화 확인

**사전조건**: TC-002 로그인 완료 상태, DevTools Network 탭 열기
**실행 단계**:
1. "+ Add Stock" 버튼 클릭
2. 티커 `AAPL` 입력, 수량 `10`, 평균가 `150` 입력
3. Save 버튼 클릭
4. Network 탭에서 `/sync/push` 요청 확인
**입력값**: Ticker = `AAPL`, Shares = `10`, Avg Cost = `150`
**예상 결과**:
- HoldingCard에 AAPL 종목이 표시됨
- Network 탭에서 `POST /sync/push` 요청 확인 (Status 200)
- 요청 body에 holdings 배열이 JSON으로 포함됨
**확인 방법**: Network 탭 > `/sync/push` 요청 > Response에 `{"success": true, "updated_at": "..."}` 확인

**결과**: [ ] Pass  [ ] Fail  **코멘트**: ___

---

### TC-004 [정상] 👤 수동 — 로그아웃 → 재로그인 → 데이터 복원

**사전조건**: TC-003 완료 (AAPL 종목 추가된 상태)
**실행 단계**:
1. 헤더의 "Logout" 버튼 클릭
2. 로그인 게이트로 돌아가는지 확인
3. CODE + PIN 입력 후 다시 로그인
4. AAPL 종목이 복원되는지 확인
**입력값**: TC-001 코드 + PIN `1234`
**예상 결과**:
- 로그아웃 시 로그인 게이트 즉시 표시
- 재로그인 시 잠시 LoadingSkeleton 표시 후 AAPL 종목 카드 복원
- 수량 10주, 평균가 $150 그대로 유지
**확인 방법**: 종목 카드의 ticker, shares, avg_cost가 TC-003에서 입력한 값과 동일

**결과**: [ ] Pass  [ ] Fail  **코멘트**: ___

---

### TC-005 [정상] 👤 수동 — Settings MY ACCOUNT 표시

**사전조건**: Portfolio에서 로그인 완료 상태
**실행 단계**:
1. Settings 페이지로 이동
2. MY ACCOUNT 섹션 확인
3. "Copy" 버튼 클릭
4. 메모장에 붙여넣기로 코드 확인
**입력값**: 없음
**예상 결과**:
- MY ACCOUNT 섹션에 MY CODE 라벨 아래 코드가 모노스페이스 폰트로 표시
- "Copy" 버튼 클릭 시 "Copied!" 텍스트로 2초간 변경
- 빨간색 테두리의 "Logout" 버튼 표시
**확인 방법**: 복사된 코드가 Portfolio 헤더의 코드와 동일

**결과**: [ ] Pass  [ ] Fail  **코멘트**: ___

---

### TC-006 [정상] 👤 수동 — Remember 체크 → 탭 닫기 → 코드 기억

**사전조건**: 로그아웃 상태, Portfolio 로그인 게이트
**실행 단계**:
1. "Remember on this browser" 체크박스 선택
2. CODE + PIN 입력 후 로그인
3. 브라우저 탭 완전 닫기
4. 새 탭에서 Portfolio 페이지 재접속
**입력값**: CODE + PIN + Remember 체크
**예상 결과**:
- 재접속 시 로그인 게이트 표시 (세션은 탭 닫으면 소멸)
- CODE 필드에 이전 코드가 자동 채워져 있음
- PIN 필드는 비어있음
- "Remember on this browser" 체크박스 선택 상태
**확인 방법**: DevTools > Application > Local Storage에 `portfolio_remember` 키 존재, 코드 값 포함

**결과**: [ ] Pass  [ ] Fail  **코멘트**: ___

---

### TC-007 [정상] 👤 수동 — 빈 포트폴리오 초기 화면

**사전조건**: 신규 코드 발급 직후 (종목 없는 상태)
**실행 단계**:
1. "Start Portfolio" 클릭하여 포트폴리오 진입
2. 빈 상태 화면 확인
**입력값**: 없음
**예상 결과**:
- "아직 추가된 종목이 없습니다." 텍스트 표시
- "첫 종목을 추가해서 포트폴리오 분석을 시작하세요." 안내 메시지 표시
- "+ Add Your First Stock" 버튼 표시 (accent 색상 배경)
**확인 방법**: 빈 상태 카드 UI가 화면 중앙에 표시됨

**결과**: [ ] Pass  [ ] Fail  **코멘트**: ___

---

### TC-008 [정상] 👤 수동 — 발급 코드 복사 버튼

**사전조건**: TC-001 Issued 뷰 상태
**실행 단계**:
1. "Copy" 버튼 클릭
2. 메모장에 Ctrl+V로 붙여넣기
**입력값**: 없음
**예상 결과**:
- 버튼 텍스트가 "Copied!"로 변경, 테두리 색상이 up(녹색)으로 변경
- 2초 후 "Copy"로 복원
- 붙여넣은 값이 화면의 코드와 동일 (XXXX-XXXX-XXXX 형식)
**확인 방법**: 클립보드 내용이 표시된 코드와 일치

**결과**: [ ] Pass  [ ] Fail  **코멘트**: ___

---

### TC-009 [엣지] 🤖 AI검증 — 코드 입력 자동 포맷팅 (XXXX-XXXX-XXXX)

**사전조건**: 로그인 게이트 Login 뷰
**실행 단계**:
1. CODE 필드에 `abcd1234efgh` 타이핑
**입력값**: `abcd1234efgh` (소문자, 하이픈 없음)
**예상 결과**:
- 입력값이 `ABCD-1234-EFGH`로 자동 변환 (대문자 + 하이픈)
- 영숫자 외 문자는 제거됨
- 최대 14자 (12자리 + 하이픈 2개)
**확인 방법**: `formatCode()` 함수 분석 — `val.replace(/[^A-Z0-9]/gi, '').toUpperCase().slice(0, 12)` 후 4자리씩 하이픈으로 결합

**AI 검증 결과**: ✅ `PortfolioLoginGate.tsx:28-31` — `formatCode` 함수가 소문자→대문자, 비영숫자 제거, 4-4-4 하이픈 분리를 수행

**결과**: [x] Pass  [ ] Fail  **코멘트**: 코드 로직 확인 완료

---

### TC-010 [엣지] 🤖 AI검증 — PIN에 문자/특수문자 입력 필터링

**사전조건**: 로그인 게이트 Login 또는 Register 뷰
**실행 단계**:
1. PIN 필드에 `12ab!@34` 입력
**입력값**: `12ab!@34`
**예상 결과**:
- PIN 필드에 `1234`만 표시 (숫자만 허용, 4자리 제한)
- 문자/특수문자는 즉시 필터링됨
**확인 방법**: `onChange` 핸들러 분석 — `e.target.value.replace(/\D/g, '').slice(0, 4)`

**AI 검증 결과**: ✅ `PortfolioLoginGate.tsx:220` (Login), `PortfolioLoginGate.tsx:321` (Register SET PIN), `PortfolioLoginGate.tsx:337` (Register CONFIRM PIN) — 3곳 모두 `/\D/g` 정규식으로 비숫자 제거 + `.slice(0, 4)` 길이 제한 적용

**결과**: [x] Pass  [ ] Fail  **코멘트**: 코드 로직 확인 완료

---

### TC-011 [엣지] 🤖 AI검증 — 불완전한 코드/PIN으로 버튼 비활성

**사전조건**: 로그인 게이트 Login 뷰
**실행 단계**:
1. 코드 13자 이하만 입력 (하이픈 포함 14자 미만)
2. Login 버튼 상태 확인
3. 코드 14자 + PIN 3자리만 입력
4. Login 버튼 상태 확인
**입력값**: CODE < 14자, PIN < 4자리
**예상 결과**:
- Login 버튼이 `disabled` 상태 (opacity 0.5, 클릭 불가)
- 코드 14자 + PIN 4자리 모두 입력해야 활성화
**확인 방법**: `disabled` 속성 분석 — `disabled={loading || code.length !== 14 || pin.length !== 4}`

**AI 검증 결과**: ✅ `PortfolioLoginGate.tsx:248` — `disabled={loading || code.length !== 14 || pin.length !== 4}` 조건 확인. Register 뷰도 `PortfolioLoginGate.tsx:350` — `disabled={loading || pin.length !== 4 || pin !== pinConfirm}` 조건 적용

**결과**: [x] Pass  [ ] Fail  **코멘트**: 코드 로직 확인 완료

---

### TC-012 [엣지] 🤖 AI검증 — Register에서 PIN 확인 불일치

**사전조건**: 로그인 게이트 Register 뷰
**실행 단계**:
1. SET PIN에 `1234` 입력
2. CONFIRM PIN에 `5678` 입력 (4자리까지 입력 완료)
**입력값**: PIN = `1234`, CONFIRM = `5678`
**예상 결과**:
- CONFIRM PIN 입력 필드 테두리가 빨간색(theme.down)으로 변경
- 필드 아래 "PINs do not match" 텍스트 빨간색 표시
- "Create" 버튼 비활성 상태
**확인 방법**: 조건부 렌더링 분석 — `pinConfirm.length === 4 && pin !== pinConfirm` 조건으로 에러 표시

**AI 검증 결과**: ✅ `PortfolioLoginGate.tsx:330-331` — borderColor 조건: `pinConfirm.length === 4 && pin !== pinConfirm ? theme.down : theme.border`. `PortfolioLoginGate.tsx:340-345` — 불일치 시 에러 메시지 표시. `PortfolioLoginGate.tsx:350` — Create 버튼 `disabled` 조건에 `pin !== pinConfirm` 포함

**결과**: [x] Pass  [ ] Fail  **코멘트**: 코드 로직 확인 완료

---

### TC-013 [엣지] 🤖 AI검증 — 서버 데이터가 빈 배열일 때 (빈 포트폴리오)

**사전조건**: 서버에 `data = '[]'` 저장된 계정으로 로그인
**실행 단계**:
1. 코드+PIN으로 로그인
2. pullSync 결과로 빈 배열 수신
**입력값**: 서버 응답 `{ data: "[]" }`
**예상 결과**:
- `initialLoaded = true`로 전환 (로딩 스켈레톤이 아닌 빈 상태 UI)
- `holdings = []` → "아직 추가된 종목이 없습니다" 화면
- LoadingSkeleton이 아닌 빈 포트폴리오 카드 표시
**확인 방법**: `usePortfolio.ts` 로직 분석 — `JSON.parse("[]")` → 빈 배열 → `initialLoaded=true` + `holdings.length===0` → 빈 상태 분기

**AI 검증 결과**: ✅ `usePortfolio.ts:43-45` — `JSON.parse(result.data)` 후 `Array.isArray` 검증 통과 → `setHoldings([])` → `setInitialLoaded(true)`. `Portfolio.tsx:108-117` — `!initialLoaded`일 때만 스켈레톤. `Portfolio.tsx:120-159` — `holdings.length === 0`일 때 빈 상태 UI

**결과**: [x] Pass  [ ] Fail  **코멘트**: 코드 로직 확인 완료

---

### TC-014 [엣지] 🤖 AI검증 — 서버 데이터가 Array가 아닐 때

**사전조건**: 서버에 손상된 데이터 저장 (예: `data = '{}'`)
**실행 단계**:
1. pullSync 결과로 `{ data: "{}" }` 수신
2. JSON.parse 후 Array 검증
**입력값**: 서버 응답 `{ data: "{}" }`
**예상 결과**:
- `Array.isArray` 검증 실패 → `throw new Error('Invalid portfolio data')`
- catch 블록에서 에러 메시지 설정
- UI에 "Invalid portfolio data" 에러 표시
- 앱 크래시 없이 안정적 처리
**확인 방법**: `usePortfolio.ts` 로직 분석 — `if (!Array.isArray(parsed)) throw new Error('Invalid portfolio data')` 코드 존재 확인

**AI 검증 결과**: ✅ `usePortfolio.ts:44` — `if (!Array.isArray(parsed)) throw new Error('Invalid portfolio data')` 검증 코드 확인. catch 블록(line:46-50)에서 에러 메시지 설정 + `initialLoaded=true` 처리

**결과**: [x] Pass  [ ] Fail  **코멘트**: 코드 로직 확인 완료

---

### TC-015 [실패] 👤 수동 — 존재하지 않는 코드로 로그인 (404)

**사전조건**: 로그인 게이트 Login 뷰
**실행 단계**:
1. CODE 필드에 `ZZZZ-ZZZZ-ZZZZ` 입력
2. PIN 필드에 `0000` 입력
3. "Login" 버튼 클릭
**입력값**: CODE = `ZZZZ-ZZZZ-ZZZZ`, PIN = `0000`
**예상 결과**:
- 에러 배너에 "동기화 코드를 찾을 수 없습니다" (또는 영문 메시지) 표시
- 에러 배너 배경색이 빨간색 반투명 (theme.down + 15% 투명도)
- 로그인 게이트 유지 (포트폴리오 진입 안 됨)
- 버튼이 다시 활성화됨 (loading 해제)
**확인 방법**: Network 탭에서 `/sync/connect` 응답 Status 404 확인, 화면에 에러 메시지 표시

**결과**: [ ] Pass  [ ] Fail  **코멘트**: ___

---

### TC-016 [실패] 👤 수동 — 틀린 PIN 3회 → 계정 잠금 (429)

**사전조건**: TC-001에서 발급받은 유효한 코드
**실행 단계**:
1. 올바른 CODE + 틀린 PIN `9999` → Login 클릭
2. 에러 메시지 확인 (남은 시도 횟수)
3. 다시 틀린 PIN `8888` → Login 클릭
4. 다시 틀린 PIN `7777` → Login 클릭
5. 잠금 메시지 확인
**입력값**: CODE = 유효한 코드, PIN = `9999` → `8888` → `7777`
**예상 결과**:
- 1회차: "PIN이 올바르지 않습니다. (남은 시도: 2회)" 표시
- 2회차: "PIN이 올바르지 않습니다. (남은 시도: 1회)" 표시
- 3회차: "30초 후 다시 시도하세요" 잠금 메시지 표시
- 30초 후 다시 시도 가능
**확인 방법**: Network 탭에서 1-2회 401 응답, 3회째 429 응답 확인

**결과**: [ ] Pass  [ ] Fail  **코멘트**: ___

---

### TC-017 [실패] 👤 수동 — 서버 다운 시 로그인 시도

**사전조건**: 백엔드 서버 종료 (uvicorn 프로세스 중지), 프론트엔드만 실행
**실행 단계**:
1. Portfolio 로그인 게이트에서 CODE + PIN 입력
2. "Login" 버튼 클릭
3. 에러 메시지 확인
**입력값**: 아무 CODE + PIN
**예상 결과**:
- "Login" 버튼이 "Logging in..."으로 변경 후 잠시 대기
- 네트워크 에러 발생 → 에러 배너에 "Failed to fetch" 또는 유사 메시지 표시
- 앱 크래시 없음, 로그인 게이트 유지
- 버튼이 다시 "Login"으로 복원
**확인 방법**: 콘솔에 네트워크 에러, 화면에 에러 메시지 표시, 앱 동작 정상

**결과**: [ ] Pass  [ ] Fail  **코멘트**: ___

---

### TC-018 [실패] 🤖 AI검증 — pushSync 실패 시 에러 표시

**사전조건**: 포트폴리오 로그인 상태, 서버에 push 실패 발생
**실행 단계**:
1. 종목 추가/수정/삭제 시 `pushSync` 호출
2. 서버가 에러 응답 반환 (네트워크 오류 또는 401)
**입력값**: pushSync 실패 응답
**예상 결과**:
- `error` 상태에 에러 메시지 설정
- 로컬 상태(holdings)는 변경된 채 유지 (optimistic update)
- ErrorBanner 컴포넌트에 에러 메시지 표시
**확인 방법**: `syncToServer` 함수의 catch 블록 분석

**AI 검증 결과**: ✅ `usePortfolio.ts:59-64` — `syncToServer` catch 블록에서 `setError(e.message)` 호출. `Portfolio.tsx:239` — `{error && <ErrorBanner message={error} onRetry={refreshQuotes} />}` 에러 배너 렌더링 확인

**결과**: [x] Pass  [ ] Fail  **코멘트**: 에러 표시 로직 존재. 단, 로컬 상태 복원은 미구현 (V2 개선 대상)

---

### TC-019 [실패] 🤖 AI검증 — pullSync 실패 시 에러 표시

**사전조건**: 포트폴리오 로그인 시도, pullSync 실패
**실행 단계**:
1. `usePortfolio` 초기 로드에서 `pullSync` 호출
2. 서버가 에러 응답 반환
**입력값**: pullSync 실패 응답
**예상 결과**:
- `error` 상태에 에러 메시지 설정
- `initialLoaded = true` (로딩 스켈레톤 해제)
- `holdings = []` (빈 배열 유지)
- ErrorBanner에 에러 메시지 표시
**확인 방법**: useEffect 내 catch 블록 분석

**AI 검증 결과**: ✅ `usePortfolio.ts:46-50` — catch 블록에서 `setError(e.message)` + `setInitialLoaded(true)` 호출. cancelled 플래그로 언마운트 시 상태 업데이트 방지

**결과**: [x] Pass  [ ] Fail  **코멘트**: 코드 로직 확인 완료

---

### TC-020 [실패] 🤖 AI검증 — 손상된 JSON 서버 응답 처리

**사전조건**: 서버가 유효하지 않은 JSON 반환
**실행 단계**:
1. pullSync 응답 `{ data: "not valid json{{{" }` 수신
2. `JSON.parse()` 실패
**입력값**: 손상된 JSON 문자열
**예상 결과**:
- `JSON.parse` SyntaxError 발생
- catch 블록에서 에러 캐치 → "Unexpected token..." 에러 메시지 설정
- `initialLoaded = true` 전환 (에러 상태로 표시)
- 앱 크래시 없이 에러 배너 표시
**확인 방법**: try/catch 구문 분석

**AI 검증 결과**: ✅ `usePortfolio.ts:40-50` — `JSON.parse(result.data)` 이 try 블록 내부에 있으므로 SyntaxError는 catch로 전달됨. catch 블록에서 `setError` + `setInitialLoaded(true)` 처리

**결과**: [x] Pass  [ ] Fail  **코멘트**: 코드 로직 확인 완료

---

## 테스트 완료 체크리스트

- [ ] 모든 정상 케이스 Pass (TC-001 ~ TC-008)
- [ ] 엣지 케이스에서 앱이 크래시 없이 처리됨 (TC-009 ~ TC-014)
- [ ] 실패 케이스에서 사용자에게 적절한 안내가 표시됨 (TC-015 ~ TC-020)
- [ ] 새로고침 후에도 Remember 코드가 유지됨 (TC-006)
- [ ] 다크/라이트 테마 전환 시 게이트 UI 정상 렌더링
- [ ] 모바일 뷰포트(375px)에서 로그인 게이트 레이아웃 정상

---

## AI 검증 요약

| 검증 유형 | 결과 |
|-----------|------|
| TypeScript 타입 체크 (`tsc --noEmit`) | ✅ 에러 없음 |
| Vite 프로덕션 빌드 | ✅ 227ms 성공 |
| 🤖 AI검증 시나리오 (8건) | ✅ 전체 Pass |
| 👤 수동 시나리오 (12건) | ⬜ 브라우저 테스트 필요 |
